server:
  # Render injects PORT (10000 by default); fall back to 8082 locally.
  port: ${PORT:8082}

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://dpg-dapro5rtqb8s73dilihg-a.oregon-postgres.render.com:5432/orderdb?sslmode=require
    username: microservices_db_mbzb_user
    password: ${DB_PASSWORD}
    hikari:
      # Small pool for the 0.1 CPU free-tier instance (each connection = a TLS handshake at boot).
      maximum-pool-size: 5
      minimum-idle: 1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
    # ProductClient uses a fixed url, not service discovery; skip the blocking registry fetch at boot.
    fetch-registry: false
