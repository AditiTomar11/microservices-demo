server:
  # Render injects PORT (10000 by default); fall back to 8761 locally.
  port: ${PORT:8761}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: eureka-server